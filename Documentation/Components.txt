App.tsx
	Where all the component are placed together

box.tsx
	A box wrapper component to hold other components.

conditions.tsx
	holds all the condition rule popups

DCGuide.tsx
	holds a table with a DC guide

EncounterTables.tsx
	File that holds all the random encounters (taken from Xanathar's guide to everything)

LootTable.tsx
	connects to the chartopia api and gets random loot based on
	monster selected

PageHeaderBox.tsx
	navbar

popUpText.tsx
	creates a screen wide pop up

randomEncounter.tsx
	holds all the random encounter options and random selection

RandomNpc.tsx
	connects to the chartopia api and gets random npc

randomTavern.tsx
	connects to the chartopia api and gets random tavern

